"""Bundled asset folder (``LLS4.blend`` + ``textures_real_lights/``).

Populated in step 7. The ``__init__.py`` exists only so the folder is a
proper Python subpackage and is recognized by tooling; it ships no code.
"""
